server.port = 8080
server.servlet.session.persistent=false

# gradebook service
gradebook.url = http://localhost:8081
gradebook.service = default
# registration service
registration.url = http://localhost:8080
registration.service = default

# URL for redirect back to React after successful login
frontend.post.login.url = http://localhost:3000/semester
# google authentication provider
#spring.security.oauth2.client.registration.google.client-id = <KEY>
#spring.security.oauth2.client.registration.google.client-secret = <KEY>

# database properties
spring.datasource.url = jdbc:mysql://localhost:3306/register
spring.datasource.username = root
spring.datasource.password = bantalkecil
# spring.jpa.hibernate.ddl-auto = update
# spring.jpa.show-sql = true

# server.servlet.session.persistent=false
spring.jpa.hibernate.ddl-auto = update
spring.datasource.hikari.maximum-pool-size=5

# debug = true
# logging.level.org.springframework.security = TRACE


# Should not store your client id and secret key in a public repository.  
# The easiest way to do this is to include the application.properties file 
# in .gitignore file.
#spring.security.oauth2.client.registration.google.client-id = <KEY>
#spring.security.oauth2.client.registration.google.client-secret = <KEY>   